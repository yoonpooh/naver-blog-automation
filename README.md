# 네이버 블로그 자동화

Codex Desktop에서 네이버 블로그용 키워드 조사부터 실제 발행까지 진행하는 개인용 자동화 스킬 모음입니다.

## 동작 환경

이 프로젝트는 다음 환경을 기준으로 만들었습니다.

- macOS
- Codex Desktop 앱
- 로그인된 Chrome의 네이버 블로그 편집기
- Codex의 Chrome 제어, Computer Use, 이미지 생성 기능

네이버 편집기와 macOS 네이티브 파일 선택기를 직접 조작하므로 일반 CLI, 헤드리스 브라우저, Windows, Linux에서는 정상 동작을 보장하지 않습니다. 현재로서는 **macOS의 Codex Desktop 앱 전용 워크플로**로 보는 편이 안전합니다.

## 자동화 순서

1. `keyword-research`: 급상승 후보와 꾸준한 저경쟁 후보를 조사해 키워드 하나를 선정합니다.
2. `title-generation`: 선정한 키워드로 제목 하나를 만듭니다.
3. `article-generation`: 공식 자료를 확인해 한국어 정보성 본문과 해시태그를 작성합니다.
4. `image-generation`: 본문의 모든 이미지 영역에 맞는 이미지를 병렬 생성합니다.
5. `article-publishing`: 로그인된 네이버 블로그 편집기에 제목, 본문, 이미지와 서식을 입력하고 검증 후 발행합니다.

## 자동화 프롬프트

현재 자동화는 한 번 실행할 때 서로 다른 키워드의 글 세 편을 준비하고 순서대로 발행합니다. Codex Desktop 자동화 프롬프트에는 아래 내용을 사용합니다.

```text
아래 5개 스킬을 순서대로 실행해 네이버 블로그 글을 총 3회 작성하고 발행해줘.

1. keyword-research
2. title-generation
3. article-generation
4. image-generation
5. article-publishing

- 세 글은 검색 의도와 핵심 주제가 겹치지 않는 서로 다른 키워드를 사용한다.
- 최근 자동화 메모리와 이번 실행에서 선택한 키워드를 확인해 중복·유사 키워드와 검색 잠식 가능성을 제외한다.
- 각 회차의 제목, 본문, 이미지, 에디터 입력과 검증을 독립적으로 완료하고 이전 회차 결과물을 재사용하지 않는다.
- 각 글은 자연스러운 한국어 정보성 문체, 소제목 5개 이상, 공식 자료 우선의 사실 검증, 연관 해시태그를 포함한다.
- 모든 이미지 영역에 섹션 내용을 즉시 식별할 수 있는 이미지를 생성하고 파일명 순서를 보장한다.
- 이미지 업로드는 사진 버튼부터 네이티브 파일 선택기의 열기까지 Computer Use만 사용한다.
- 소제목은 각각 Bold와 19px를 적용하고, 이미지 순서·AI 활용 설정·해시태그·카테고리·잔여 마커를 최종 검증한다.
- 1회차는 즉시 발행하고 실제 성공 시각을 T0로 기록한다.
- 2회차는 T0+6시간, 3회차는 T0+12시간에 예약 발행한다.
- 네이버가 목표 분을 제공하지 않으면 목표보다 빠르지 않은 가장 가까운 시각을 선택한다.
- 1회차의 제목과 공개 URL을 확인하지 못하면 T0를 추정하거나 2·3회차를 진행하지 않는다.
- 각 회차의 최종 발행 버튼은 정확히 한 번만 누른다. 결과가 불분명하면 다시 누르지 않는다.
- 예약 글은 isAfterWrite=true 리디렉션, logNo와 예약 날짜·시각을 확인하면 성공으로 처리한다.
- 로그인, 권한 또는 필수 자료 누락처럼 직접 진행할 수 없는 문제에서만 중단한다.

마지막에는 T0와 각 회차의 키워드, 제목, 소제목 수, 이미지 수, AI 활용 설정 수,
해시태그 포함 여부, 카테고리, 발행 또는 예약 시각, 성공 여부와 게시글 URL을 보고한다.
```

## 준비

네이버 검색광고 키워드 도구를 사용하려면 `.env.example`을 `.env`로 복사하고 아래 값을 입력합니다.

```dotenv
NAVER_SEARCHAD_API_KEY=
NAVER_SEARCHAD_SECRET_KEY=
NAVER_SEARCHAD_CUSTOMER_ID=
```

그다음 Codex Desktop에서 이 저장소를 열고 다섯 스킬을 위 순서대로 실행하도록 요청하면 됩니다. 네이버 로그인이나 macOS 권한 승인이 필요할 때만 사용자 조작이 필요합니다.

## 발행 안전장치

- 이미지 업로드는 `사진` 클릭부터 네이티브 파일 선택기의 `열기`까지 Computer Use만 사용합니다.
- 이미지와 본문은 고유 마커와 컴포넌트 ID를 기준으로 배치합니다.
- 소제목 서식, 이미지 순서, AI 활용 설정, 해시태그와 카테고리를 발행 전에 검증합니다.
- 최종 발행 버튼은 한 번만 누르고, 게시글 화면의 제목과 URL이 확인돼야 성공으로 처리합니다.
- 생성된 글, 이미지와 업로드용 파일은 `output/`에 저장되며 Git에는 포함하지 않습니다.

## 참고

네이버 편집기 UI나 Codex Desktop의 브라우저 제어 방식이 바뀌면 스킬의 선택자와 작업 순서를 조정해야 할 수 있습니다. 실제 발행 전에는 로그인된 계정과 공개 범위를 확인하세요.
